import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.region.movies',
  appName: 'movies',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
